{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"],
      "env": [
        "PORT",
        "ACCESS_PUBLIC_KEY",
        "ACCESS_PRIVATE_KEY",
        "GOOGLE_CLIENT_ID",
        "GOOGLE_CLIENT_SECRET",
        "SERVER_URL",
        "GITHUB_CLIENT_ID",
        "GITHUB_CLIENT_SECRET",
        "S3_BUCKET_NAME",
        "S3_BUCKET_REGION",
        "S3_BUCKET_ACCESS_KEY",
        "S3_BUCKET_SECRET",
        "CLIENT_URL"
      ]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "db:generate": {
      "cache": false
    },
    "db:push": {
      "cache": false
    },
    "db:push-reset": {
      "cache": false
    }
  }
}
